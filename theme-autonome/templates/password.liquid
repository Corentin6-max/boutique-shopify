<div class="sw sw-block"><div class="sw__wrap sw__wrap--narrow" style="text-align:center;">
  <h1 class="sw__h2">{{ shop.name }}</h1>
  <p class="sw__lede">Boutique bientôt ouverte.</p>
  {%- form 'storefront_password' -%}
    <input type="password" name="password" placeholder="Mot de passe" class="sw-basic-form__select">
    <button type="submit" class="sw__btn sw__btn--primary">Entrer</button>
  {%- endform -%}
</div></div>
