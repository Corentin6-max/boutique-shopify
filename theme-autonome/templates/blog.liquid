<div class="sw sw-block"><div class="sw__wrap sw__wrap--narrow">
  <h1 class="sw__h2">{{ blog.title }}</h1>
  <ul class="sw-rte">
    {%- for article in blog.articles -%}
      <li><a href="{{ article.url }}">{{ article.title }}</a></li>
    {%- endfor -%}
  </ul>
</div></div>
