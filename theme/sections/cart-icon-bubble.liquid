{%- comment -%}
  Rendered on its own through the Section Rendering API so the header cart
  count can refresh without a page reload.
{%- endcomment -%}
{% render 'cart-icon-bubble' %}
{% schema %}
{ "name": "Cart count" }
{% endschema %}
